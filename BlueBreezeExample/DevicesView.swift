import SwiftUI

struct DevicesView: View {
    @EnvironmentObject var viewModel: ViewModel
    
    var body: some View {
        VStack {
            Text("Here the scanner")
        }
        .padding()
    }
}
